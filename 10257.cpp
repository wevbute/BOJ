#include <bits/stdc++.h>
#define __bpc __builtin_popcount
#define __bpcl __builtin_popcountll

using namespace std;

typedef unsigned long long int llu;
typedef long long int lli;
template <typename _A, typename _B> using _p = pair<_A, _B>;
template <typename _A> using _v = vector<_A>;
template <typename _A> using _q = queue<_A>;
template <typename _A> using _s = stack<_A>;
template <typename _A> using _d = deque<_A>;
template <typename _A> using _pq = priority_queue<_A>;
template <typename _A, typename _C> using _pqc = priority_queue<_A, _v<_A>, _C>;

int _T, _N, _M;

int tmain(void)
{
    scanf("%d%d", &_N, &_M);
    return 0;
}

int main(void)
{
    scanf("%d", &_T);
    while (_T--) tmain();
    return 0;
}